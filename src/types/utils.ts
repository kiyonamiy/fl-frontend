export type Utils = {
    client: number,
    preClient: number,
    round: number,
    preRound: number,
    left: number,
};

export const DEFAULT_UTILS: Utils = {
    client: -1,
    preClient: -1,
    round: -1,
    preRound: -1,
    left: -1
};
